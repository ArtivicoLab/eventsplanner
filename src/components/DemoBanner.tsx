// A slim, always-visible bar shown whenever demo mode is on, so it's obvious
// you're looking at sample events — and obvious how to leave it.
// TEMPORARILY DISABLED for Etsy listing screenshots — say the word and the
// `return null;` below comes back out.
export function DemoBanner() {
  return null;
}
